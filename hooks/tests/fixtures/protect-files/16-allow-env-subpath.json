{
  "name": "allow deep/.env -- blocking moved to the permission-layer deny rules",
  "stdin": { "tool_input": { "file_path": "apps/web/.env" } },
  "expect_exit": 0,
  "expect_stdout_not_contains": ["deny"]
}
