{
  "name": "allow .env.local file -- blocking moved to the permission-layer deny rules",
  "stdin": { "tool_input": { "file_path": "/app/.env.local" } },
  "expect_exit": 0,
  "expect_stdout_not_contains": ["deny"]
}
